# java-calculator-precourse

문자열을 입력 받아서 구분자가 입력되기 전까지 숫자를 받는다.
구분자가 입력되면 받은 숫자를 integer로 바꾸고 다음 숫자를 받는다.
다음 숫자도 받으면 똑같이 integer로 바꾸고 이전의 것에 더한다.
문자열이 끝나면 결과를 출력한다.

//가 입력되면 커스텀 구분자로 인식하고 커스텀 구분자를 일반 구분자로 바꾼다.

사용자가 잘못된 값을 입력할 경우 IllegalArgumentException을 발생시킨다.